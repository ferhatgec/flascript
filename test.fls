var(string) -> Fegeya FlaScript -> project name <-

#project_name ->
	if[var(string) -> project name <- (==) var(string) -> Fegeya FlaScript <-] -> {
		print(string) -> "Passed"
	} else -> {
		print(string) -> "Failed"
	} <-
#project_name <-

() -> main {
	statement[#project_name]
}
