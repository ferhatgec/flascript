#check ->
	if[is_exist(var(is_scrift_exist))] -> {
		print(string) -> "Yeey!"
	} else -> {
		print(string) -> "Why?" 
	} <-
#check <-

main() -> main {
    var(string) -> /bin/fla -> is_scrift_exist <-
    
    statement[#check]
    
    print(newline)
} main;
