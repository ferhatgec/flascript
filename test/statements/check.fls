#checkroot ->
	if[var(system) -> check[whoami] <- (==) var(string) -> root <-] -> {
		print(string) -> "You are Super user! Yey! :^)"
	} else -> {
		print(string) -> ":) :) :)"
	} <-
#checkroot <-

main() -> main {
	statement[#checkroot]
} main;
