#if LINUX
	print(string) -> "Hi, GNU/Linux user!\n"
#endif LINUX

#if FREEBSD
	print(string) -> "Hi, FreeBSD user!\n"
#endif FREEBSD

main() -> main {
	#if OPENBSD
		// This should give an error.	
	#endif OPENBSD
	
	exit(success)
	
	var(int) -> 12 -> number <-
	var(int) -> null -> data <-
	
	print(string) -> "Factorial(𝑥)\n𝑥 = " 
	
	@input(int) -> data <
	
	// factorial(5) etc.
	@number !!= var(data) <

	print(string) -> "Factorial({data}) = {number}\n"
} main;	
